<template name="toast">
  <view class='toast-content-box' wx:if="{{isHide}}">
    <view class='toast-content'>
      <view class='toast-content-text'>{{content}}</view>
    </view>
  </view>
</template>